import PolymerRedux from './redux-behaviour';

describe('Store', () => {
  it('should know how to add a friend', () => {
    let currentState = PolymerRedux.getState();
    expect(true).toBeTrue();
  });
});
